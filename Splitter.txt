<div
  ref={chatDisplayRef}
- className={`flex-grow flex flex-col items-center px-4 overflow-y-auto pt-24 scroll-smooth ${messages.length === 0 ? 'justify-center' : ''}`}>
 className={`flex-grow flex flex-col items-center px-4 overflow-y-auto pt-24 scroll-smooth ${messages.length === 0 ? 'justify-center' : ''}`}
 onScroll={handleScroll}>
