import os
import io
import fitz  # PyMuPDF
import pdfplumber
from paddleocr import PaddleOCR
from pdf2image import convert_from_path

# Khởi tạo PaddleOCR cho tiếng Việt (CPU)
ocr = PaddleOCR(use_angle_cls=True, lang="vi")  # hoặc "en" nếu là tiếng Anh

def read_pdf_with_paddleocr(path):
    all_text = []

    # Mở PDF bằng fitz
    doc = fitz.open(path)

    # Mở thêm bằng pdfplumber để lấy bảng
    with pdfplumber.open(path) as plumber_pdf:
        for i, page in enumerate(doc):
            page_text = f"--- Page {i+1} ---\n"

            # Lấy text cơ bản
            text = page.get_text().strip()

            # Nếu không có text (PDF dạng scan)
            if not text:
                print(f"[OCR] Running PaddleOCR on page {i+1}...")
                images = convert_from_path(path, first_page=i+1, last_page=i+1)

                # Chuyển ảnh sang bytes
                img = images[0]
                buf = io.BytesIO()
                img.save(buf, format="PNG")
                img_bytes = buf.getvalue()

                # OCR bằng PaddleOCR
                result = ocr.ocr(img_bytes, cls=True)

                ocr_text = []
                for line in result[0]:
                    ocr_text.append(line[1][0])  # text
                text = "\n".join(ocr_text)

            page_text += text + "\n"

            # Lấy bảng (nếu có)
            plumber_page = plumber_pdf.pages[i]
            tables = plumber_page.extract_tables()
            for t_index, table in enumerate(tables, start=1):
                page_text += f"\n[Table {t_index} on Page {i+1}]\n"
                for row in table:
                    page_text += " | ".join(str(cell) if cell else "" for cell in row) + "\n"

            all_text.append(page_text)

    return "\n\n".join(all_text)


# 🚀 Usage
if __name__ == "__main__":
    pdf_file = "example.pdf"  # thay bằng file của bạn
    if not os.path.exists(pdf_file):
        print(f"File {pdf_file} không tồn tại!")
    else:
        content = read_pdf_with_paddleocr(pdf_file)
        with open("extracted.txt", "w", encoding="utf-8") as f:
            f.write(content)
        print("✅ Extracted content saved to extracted.txt")
