const rawMarkdown = node.children.reduce((acc, child) => {
if (child.type === 'text') {
return acc + child.value;
if (child. type === 'paragraph' && child.children.length > 0) {
// Handle cases where a paragraph contains a single text
node
return acc + child.children[0].value;
3,
｝
// For other complex nodes like lists, return the full text value
return acc + (child.value || '');
'');
