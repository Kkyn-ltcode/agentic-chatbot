import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig(({ command, mode, isSsrBuild, isPreview }) => {
  const base = process.env.NB_PREFIX || '/';

  return {
    plugins: [react()],
    base: base,
    server: {
      host: true, // This is essential for external access in a containerized environment
    },
  };
});
