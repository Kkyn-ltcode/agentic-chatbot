import polars as pl

# Example DataFrame
df = pl.DataFrame({
    "group": ["A", "A", "B", "B", "B", "C"],
    "value": [1, 2, 3, 4, 5, 6],
})

# Group by "group" and aggregate "value" as list
result = (
    df.group_by("group")
      .agg(pl.col("value").list())
)

print(result)
