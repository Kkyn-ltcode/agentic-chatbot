version: 1
disable_existing_loggers: False

formatters:
  custom:
    format: "[%(asctime)s] %(message)s"
    datefmt: "%Y-%m-%d %H:%M:%S"

filters:
  block_endpoints:
    (): my_logging.EndpointFilter
    prefixes:
      - "/conversation/"
      - "/chat/"
    extra_block:
      - "GET /health"
      - "GET /favicon.ico"

handlers:
  console:
    class: logging.StreamHandler
    formatter: custom
    filters: [block_endpoints]
    stream: ext://sys.stdout

loggers:
  uvicorn.access:
    level: INFO
    handlers: [console]
    propagate: False

root:
  level: INFO
  handlers: [console]
