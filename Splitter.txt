import React from 'react';

const chatInputContainerStyle = {
  display: 'flex',
  flexDirection: 'column',
  padding: '12px',
  backgroundColor: '#f7f6f4',
  borderRadius: '20px',
  boxShadow: '0 4px 6px rgba(0, 0, 0, 0.05)',
  border: '1px solid #e1e0de',
  maxWidth: '1900px',
  fontFamily:
    '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif',
};

const topLayerStyle = {
  paddingBottom: '10px',
};

const chatInputFieldStyle = {
  width: '100%',
  border: 'none',
  background: 'transparent',
  outline: 'none',
  fontSize: '16px',
  color: '#333',
};

const bottomLayerStyle = {
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  paddingTop: '10px',
};

const chatInputLeftStyle = {
  display: 'flex',
  alignItems: 'center',
  gap: '8px',
};

const chatInputRightStyle = {
  display: 'flex',
  alignItems: 'center',
  gap: '8px',
};

const iconButtonStyle = {
  backgroundColor: 'transparent',
  border: '1px solid #d4d4d4',
  borderRadius: '8px',
  width: '36px',
  height: '36px',
  fontSize: '24px',
  color: '#666',
  cursor: 'pointer',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  transition: 'background-color 0.2s',
};

const separatorIconStyle = {
  fontSize: '16px',
  fontWeight: 'bold',
};

const modelSelectStyle = {
  display: 'flex',
  alignItems: 'center',
  gap: '4px',
  color: '#666',
  fontSize: '14px',
  cursor: 'pointer',
};

const dropdownArrowStyle = {
  fontSize: '12px',
  transform: 'rotate(180deg)',
};

const sendButtonStyle = {
  backgroundColor: '#e5b6a7',
  color: 'white',
  border: 'none',
  borderRadius: '12px',
  width: '40px',
  height: '40px',
  fontSize: '20px',
  cursor: 'pointer',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  transition: 'background-color 0.2s',
};

// Note: Inline styles can't handle pseudo-selectors like `:hover`
// For hover effects, you would need to use a library like styled-components
// or use state to dynamically change the style on mouse enter/leave.

const ChatInputBox = () => {
  return (
    <div style={chatInputContainerStyle}>
      <div style={topLayerStyle}>
        <input
          type='text'
          placeholder='Reply to Claude...'
          style={chatInputFieldStyle}
        />
      </div>
      <div style={bottomLayerStyle}>
        <div style={chatInputLeftStyle}>
          <button style={iconButtonStyle}>+</button>
          <button style={{ ...iconButtonStyle, ...separatorIconStyle }}>
            ⇄
          </button>
        </div>
        <div style={chatInputRightStyle}>
          <div style={modelSelectStyle}>
            <span>Claude Sonnet 4</span>
            <span style={dropdownArrowStyle}>↓</span>
          </div>
          <button style={sendButtonStyle}>↑</button>
        </div>
      </div>
    </div>
  );
};

export default ChatInputBox;
